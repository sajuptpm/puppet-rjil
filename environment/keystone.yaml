resources:
  bootstrap:
    number: 1
    flavor: small
    image: trusty
    networks: default
    boot_volume: default_volume
    assign_floating_ip: true
  keystonedb:
    number: 1
    flavor: medium
    image: trusty
    networks: default
    boot_volume: default_volume
    assign_floating_ip: true
